{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "JWT_SECRET",
      "description": "JWT signing secret; a generated development value is in use. Replace it for production.",
      "requiredAtBoot": true,
      "generatedForDevelopment": true
    },
    {
      "name": "EMAIL_HOST",
      "description": "Optional SMTP server hostname for email notifications.",
      "requiredAtBoot": false
    },
    {
      "name": "EMAIL_PORT",
      "description": "Optional SMTP server port for email notifications.",
      "requiredAtBoot": false
    },
    {
      "name": "EMAIL_USER",
      "description": "Optional SMTP username for email notifications.",
      "requiredAtBoot": false
    },
    {
      "name": "EMAIL_PASS",
      "description": "Optional SMTP password or app password for email notifications.",
      "requiredAtBoot": false
    },
    {
      "name": "EMAIL_FROM",
      "description": "Optional sender address for email notifications.",
      "requiredAtBoot": false
    },
    {
      "name": "CLOUDINARY_CLOUD_NAME",
      "description": "Optional Cloudinary cloud name for file uploads.",
      "requiredAtBoot": false
    },
    {
      "name": "CLOUDINARY_API_KEY",
      "description": "Optional Cloudinary API key for file uploads.",
      "requiredAtBoot": false
    },
    {
      "name": "CLOUDINARY_API_SECRET",
      "description": "Optional Cloudinary API secret for file uploads.",
      "requiredAtBoot": false
    }
  ]
}